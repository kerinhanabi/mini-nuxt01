export default defineEventHandler((event) => {

    //handle custom query params example in about page

    return {
        //message: 'Hello from custom query params ${ name }'
        message: 'Hello from mini self custom API'
    }
})